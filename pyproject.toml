[project]
name = "ccusr-fruit-grading"
version = "0.1.0"
description = "进口水果批次分级追溯系统:证据链、抽样规则、结论版本化、放行裁决与箱号审计"
requires-python = ">=3.11"
dependencies = []

[tool.unittest]
start-directory = "tests"

